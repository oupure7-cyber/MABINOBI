# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json

# Codex local settings (if ever created inside the repo)
.codex/

# OS
Thumbs.db
.DS_Store

# PyInstaller build artifacts (마비노비.exe is committed on purpose; these are just build leftovers)
build/
dist/
*.spec
!50_APP/mabinobi.spec
# Local rollback copies; only the main executable is distributed.
/마비노비.*.exe

# Per-character data snapshots (personal game data, not shared/committed)
/character_data/

# Per-machine MabinogiMobile_CLI.exe location (varies per user's install, not shared)
/cli_settings.json

# Local release downloads and rollback archives
/MabiNobi.exe
/*.exe.bak
/*.7z
/보유수량-업데이트.txt
